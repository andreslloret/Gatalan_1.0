<h1>Edit Tour</h1>

<%= form_with(model: @tour, url: tour_path(@tour), method: :patch) do |form| %>
  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control" %>
